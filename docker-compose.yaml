version: "3.7"
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
  nginx:
    build:
      context: .
      dockerfile: Dockerfile.nginx
    ports:
      - '80:3000'
      - '443:3000'
    volumes:
      - ./ssl:/etc/nginx/ssl